lib:
  activateWorkflowUi: false

servers:
  testExternalJar:
    type: externalJar
    source:
      - local:../tiger-testenv-mgr/target/tiger-httpbin.jar
    healthcheckUrl: http://127.0.0.1:${free.port.0}
    externalJarOptions:
      arguments:
        - -port=${free.port.0}

