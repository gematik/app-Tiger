requestCriterions:
  - message.method == 'POST'
  - message.url =~ '.*/specificEndpoint'
response:
  statusCode: 203
  headers:
    content-type: application/json
  body: '{
          "authorizedUser": "?{$.body.body.username.content}"
        }'
