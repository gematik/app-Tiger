${package}
import io.cucumber.junit.CucumberOptions;
import de.gematik.test.tiger.TigerCucumberRunner;
import org.junit.runner.RunWith;

@RunWith(TigerCucumberRunner.class)
@CucumberOptions(
    features = {"${feature}"},
    plugin = {"json:target/cucumber-parallel/${ctr}.json", "de.gematik.test.tiger.TigerCucumberListener"},
    glue = { ${glues} },
    tags = "not @Ignore")
public class ${driverClassName} {
}
