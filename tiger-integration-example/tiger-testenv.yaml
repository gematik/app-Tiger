servers:
  winstone:
    hostname: winstone
    source:
      - local:target/winstone.jar
    type: externalJar
    externalJarOptions:
      healthcheck: http://localhost:${free.port.0}
      workingDir: target
      arguments:
        - --httpPort=${free.port.0}
        - --webroot=.
