diff --git a/node_modules/eslint/lib/cli-engine/formatters/stylish.js b/node_modules/eslint/lib/cli-engine/formatters/stylish.js
index 18aa581..0c8fbec 100644
--- a/node_modules/eslint/lib/cli-engine/formatters/stylish.js
+++ b/node_modules/eslint/lib/cli-engine/formatters/stylish.js
@@ -4,8 +4,7 @@
  */
 "use strict";
 
-const chalk = require("chalk"),
-	util = require("node:util"),
+const util = require("node:util"),
 	table = require("../../shared/text-table");
 
 //------------------------------------------------------------------------------
@@ -26,7 +25,11 @@ function pluralize(word, count) {
 // Public Interface
 //------------------------------------------------------------------------------
 
-module.exports = function (results) {
+let chalkPromise = import('chalk');
+
+module.exports = async function (results) {
+	const chalkModule = await chalkPromise;
+	const chalk = chalkModule.default;
 	let output = "\n",
 		errorCount = 0,
 		warningCount = 0,
@@ -46,7 +49,7 @@ module.exports = function (results) {
 		fixableErrorCount += result.fixableErrorCount;
 		fixableWarningCount += result.fixableWarningCount;
 
-		output += `${chalk.underline(result.filePath)}\n`;
+		output += `${chalk.bold.underline(result.filePath)}\n`;
 
 		output += `${table(
 			messages.map(message => {
