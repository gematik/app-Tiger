tigerProxy:

servers:
  testPkiKeysWithMissingCertificate:
    type: tigerProxy
    tigerProxyConfiguration:
      adminPort: ${free.port.2}
      proxyPort: ${free.port.3}
    pkiKeys:
    - id: disc_sig
      type: Certificate
      pem:
