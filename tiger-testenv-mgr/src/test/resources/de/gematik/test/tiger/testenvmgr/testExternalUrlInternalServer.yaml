tigerProxy:

servers:
  testExternalUrlInternalServer:
    hostname: testExternalJarInternalServer
    type: externalUrl
    source:
      - https://build.top.local
    externalJarOptions:
      healthcheck: https://build.top.local
